---
message: "This node is using common data"

classes:
  - 'profile::base'

ntp::servers:
  - 0.us.pool.ntp.org
  - 1.us.pool.ntp.org

hapcat::package_ensure: '0.0.2'
hapcat::package_pip_install_args: '--upgrade'
hapcat::api_port: 8080
